## 6. VK API ИНТЕГРАЦИЯ (АКТУАЛЬНО)\n\n### 6.1 Callback API Events\n\n| Event Type | Обработчик | Метод обработки | Время обработки (в `doPost`) |\n|------------|------------|-----------------|-------------------------------|\n| `confirmation` | `doPost()` | Синхронно | < 100ms |\n| `wall_post_new` | `enqueueEvent()` -> `processEventQueue()` -> `routeEvent()` -> `handleWallPostNew()` | Асинхронно | < 2 сек (ответ VK), ~1-3 мин (полная обработка) |\n| `wall_reply_new` | `enqueueEvent()` -> `processEventQueue()` -> `routeEvent()` -> `handleWallReplyNew()` | Асинхронно | < 2 сек (ответ VK), ~1-3 мин (полная обработка) |\n| `message_new` | `enqueueEvent()` -> `processEventQueue()` -> `routeEvent()` -> `handleMessageNew()` | Асинхронно | < 2 сек (ответ VK), ~1-3 мин (полная обработка) |\n\n**Критическое требование:** Все обработчики `doPost()` должны ответить `\"ok\"` за < 5 секунд (наша гарантия: < 2 сек). Вся сложная логика вынесена в асинхронную обработку через `EventQueue`.\n\n---\n\n### 6.2 VK API Methods\n\n| Метод | Назначение | Частота | Rate Limit | Обработка |\n|-------|------------|---------|------------|-----------|\n| `messages.send` | Уведомления, сводки | ~100-150/аукцион | 3/сек | `NotificationQueue` + `processNotificationQueue()` с задержкой |\n| `wall.createComment` | Комментарий с победителем, симулятор | ~15-30/аукцион + симулятор | 3/сек | `NotificationQueue` + `processNotificationQueue()` с задержкой / напрямую в симуляторе |\n| `groups.getById` | Получение информации о группе | По требованию | 3/сек | Напрямую через `callVk` |\n| `users.get` | Получение ФИО пользователя | ~15/аукцион | 3/сек | Напрямую через `callVk` |\n| `wall.post` | Публикация постов симулятором | ~1-5/час (для симулятора) | 50/день (для сообществ) | Напрямую через `callVk` в симуляторе |\n\n**Защита от 429 (Too Many Requests):**\n-   Исходящие уведомления и комментарии через `NotificationQueue` обрабатываются с искусственной задержкой `Utilities.sleep(350);` для соблюдения лимита (≈2.85 запроса/сек).\n-   На уровне `callVk` используется `muteHttpExceptions: true` для обработки ошибок, но явной логики `retry` с `exponential backoff` сейчас нет (это потенциальное улучшение).\n\n---\n\n## 7. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ (АКТУАЛЬНО)\n\n### 7.1 Производительность\n\n| Метрика | Требование | Обоснование |\n|---------|------------|-------------|\n| **Ответ VK Callback** | < 2 секунды | VK лимит 5 сек, запас ×2.5 за счет асинхронной очереди `EventQueue` |\n| **Обработка ставки** | < 1.5 секунды | `LockService` + запись в Sheets (`EventQueue` -> `handleWallReplyNew`) |\n| **Задержка уведомления** | 1-3 минуты | Асинхронная очередь `NotificationQueue`, обработка каждую минуту, дебонс |\n| **Завершение аукциона** | < 5 минут на 15 лотов | Определение победителей + уведомления |\n| **VK API Rate Limit** | ≤ 2.85 запроса/секунду | VK лимит 3/сек, безопасный запас при отправке уведомлений |\n| **Concurrent ставок** | До 30 одновременно | GAS лимит 30 `doPost` concurrent |\n\n---\n\n### 7.2 Безопасность (ФЗ-152)\n\n**Что хранится:**\n- ✅ VK User ID (технический идентификатор).\n- ✅ Суммы ставок.\n- ✅ Timestamp событий.\n- ✅ Comment ID (для связи).\n- ❌ **НЕ хранится:** ФИО, телефоны, email, адреса (за исключением реквизитов для оплаты в `Settings` и `PropertiesService`).\n\n**ФИО запрашивается динамически** через VK API только для отчётов и сразу отбрасывается (не сохраняется).\n\n**Политика конфиденциальности** (для группы):\n```\nПри участии в аукционе обрабатываются: технический ID VK, суммы ставок, timestamp.\nНЕ храним: ФИО, телефоны, email, адреса на серверах.\nОснование: исполнение договора (ч. 5 ст. 6 ФЗ-152).\nУдаление данных: написать \"УДАЛИТЬ МОИ ДАННЫЕ\" в ЛС (обработка в течение 3 дней).\n```\n\n---\n\n### 7.3 Надёжность\n\n**Отказоустойчивость:**\n- ✅ `EventQueue` предотвращает потерю событий при тайм-ауте `doPost`.\n- ✅ `NotificationQueue` предотвращает потерю уведомлений при временных сбоях VK API или лимитах.\n- ✅ `LockService` при обработке ставок защищает от race conditions.\n- ✅ Всегда ответ `\"ok\"` VK (даже при ошибке в `doPost`).\n- ✅ Логирование всех ошибок в лист `Журнал` и `Logger` (доступно 28 дней в GAS).\n\n**Мониторинг:**\n- ✅ Ежедневная проверка триггеров (активны ли) может быть добавлена.\n- ✅ Лист `Статистика` для истории ключевых событий системы и симулятора.\n- ✅ `logError` для критических ошибок.\n\n---\n\n## 8. EDGE CASES (АКТУАЛЬНО)\n\n| Сценарий | Поведение | Обоснование/Решение |\n|----------|-----------|---------------------|\n| **Два пользователя — одинаковая ставка** | Побеждает первый (по `timestamp`) | Честность, FIFO (реализовано в `finalizeAuction` путем сортировки) |\n| **Ставка после окончания дедлайна** | Игнорируется | Проверка `new Date() > new Date(lot.deadline)` в `validateBid` |\n| **Ставка превышает `max_bid`** | Отклоняется, пользователь получает уведомление (потенциально) | Проверка `bid > settings.max_bid` в `validateBid` |\n| **Ставка ниже текущей цены** | Отклоняется, пользователь получает дружелюбное ЛС | Проверка `bid <= currentPrice` в `validateBid`, уведомление `low_bid` |\n| **Ставка не кратна шагу** | Отклоняется, пользователь получает уведомление (потенциально) | Проверка `(bid - Number(lot.start_price)) % Number(settings.bid_step || 50) !== 0` в `validateBid` |\n| **Пользователь удалил комментарий** | Ставка остаётся в базе (`status = valid`) | Комментарий зафиксирован VK событием до удаления |\n| **Пользователь заблокировал ЛС** | Ошибка 901 логируется, обработка продолжается | Не критично, логируется в `Журнал` |\n| **VK API 429 (Too Many Requests)** | Запросы в `NotificationQueue` ставятся в очередь и отправляются с задержкой | Регулирование скорости через `Utilities.sleep(350)` в `processNotificationQueue` |\n| **`LockService` timeout** | Ставка пропускается, лог ошибки | Таймаут `5` секунд в `handleWallReplyNew`, логирование |\n| **Несколько постов с одинаковым номером лота** | Обрабатывается первый найденный, уникальность по `post_id` | `findLotByPostId` использует `post_id` как основной ключ |\n| **Комментарий \"550 рублей на другой лот\"** | Обрабатывается как ставка 550 | Парсинг по числу, не по смыслу. Уточнение: лот определяется по `post_id` комментария, а не по тексту. |\n| **Админ меняет победителя в таблице** | Не обрабатывается автоматически | Отсутствие `onChange` триггера для листа `Winners` в текущей реализации (потенциальное улучшение) |\n| **Ставка перед дедлайном (окно продления)** | Дедлайн лота автоматически продлевается | Логика в `handleWallReplyNew`, `AUCTION_EXTENSION_WINDOW_MINUTES` = 10 мин, `AUCTION_EXTENSION_DURATION_MINUTES` = 10 мин |\n\n---\n\n## 9. РАЗВЁРТЫВАНИЕ (АКТУАЛЬНО)\n\n### 9.1 Чеклист\n\n**Шаг 1: Создание Google Таблицы и проекта Apps Script**\n1.  Создать новую Google Таблицу.\n2.  Перейти в меню **Расширения** → **Apps Script**.\n3.  Скопировать содержимое следующих файлов в соответствующие файлы скрипта:\n    *   `Code.gs`\n    *   `Sheets.gs`\n    *   `VkApi.gs`\n    *   `Monitoring.gs`\n    *   `AuctionSimulator.gs`\n    *   `appsscript.json`\n4.  В Google Sheets открыть меню **VK Auction** -> **Мастер настройки** для создания всех необходимых листов и установки начальных настроек.\n\n**Шаг 2: Настройка VK Callback API**\n1.  Перейти в настройки вашего сообщества ВКонтакте.\n2.  Выбрать раздел **Работа с API** → **Callback API**.\n3.  Создать новый Callback API сервер.\n4.  URL сервера: Скопировать URL из Apps Script после публикации (см. Шаг 3). Например: `https://script.google.com/macros/s/.../exec`.\n5.  Версия API: 5.131 (или выше).\n6.  Включить следующие типы событий:\n    *   `wall_post_new` (новые записи на стене)\n    *   `wall_reply_new` (комментарии к записям)\n    *   `message_new` (личные сообщения)\n7.  Сохранить **Secret key** и **Confirmation string**.\n\n**Шаг 3: Публикация веб-приложения (Deploy Web App)**\n1.  В редакторе Apps Script нажать **Deploy** → **New deployment**.\n2.  Выбрать тип: **Web app**.\n3.  Настройки:\n    *   Execute as: **Me**\n    *   Who has access: **Anyone**\n4.  Скопировать URL веб-приложения и использовать его в настройках Callback API (см. Шаг 2).\n\n**Шаг 4: Первоначальная настройка бота**\n1.  В Google Sheets открыть меню **VK Auction**.\n2.  Выбрать **Настройка авторизации (VK)**.\n3.  Ввести необходимые параметры, такие как `VK_TOKEN`, `GROUP_ID`, `CONFIRMATION_CODE`, `VK_SECRET`, `PAYMENT_PHONE`, `PAYMENT_BANK`, `ADMIN_PASSWORD` (по желанию). Эти данные сохраняются в `PropertiesService`.\n4.  В листе **Настройки** (`Settings`) заполнить общие параметры (см. `README.md` раздел \"5. Настройка шаблонов и общих параметров\").\n\n**Шаг 5: Проверка и активация триггеров**\n1.  Убедиться, что активны следующие триггеры (проверяется через **Расширения** → **Apps Script** → **Триггеры**):\n    *   `processEventQueue()` — Time-based, Every minute.\n    *   `processNotificationQueue()` — Time-based, Every minute.\n    *   `finalizeAuction()` — Time-based, Every week Saturday 21:00.\n    *   `runSingleSimulation()` — Time-based, Every hour (если включена ежечасная симуляция через меню).\n\n**Шаг 6: Тестирование с симулятором**\n1.  Используйте меню **VK Auction → СИМУЛЯТОР → Запустить один цикл симуляции**.\n2.  Проверьте в VK, что опубликован тестовый пост-лот.\n3.  Наблюдайте за комментариями под постом (имитация ставок).\n4.  Проверьте листы **\"Лоты\"**, **\"Ставки\"**, **\"Очередь\"**, **\"Статистика\"** на корректность записи данных и постановки уведомлений в очередь.\n5.  Проверьте свои ЛС в VK на предмет получения уведомлений о низких/перебитых ставках.\n6.  Запустите `finalizeAuction()` вручную через меню **VK Auction → Ручное управление → Завершить аукцион**.\n7.  Проверьте листы **\"Победители\"** и комментарии под постами на корректность определения победителя.\n\n**Шаг 7: Первый боевой аукцион**\n1.  Клиент публикует посты с `#аукцион`.\n2.  Мониторинг в real-time через листы **\"Лоты\"**, **\"Ставки\"**, **\"Статистика\"**.\n3.  Фиксация ошибок (если есть) в листе **\"Журнал\"**.\n4.  В 21:00 — автоматическое завершение.\n5.  Проверка корректности победителей.\n\n---\n\n### 9.2 Контрольный список перед запуском\n\n- [x] Код скопирован в проект Apps Script (включая `Monitoring.gs`, `AuctionSimulator.gs`).\n- [x] Через меню **VK Auction -> Мастер настройки** созданы все листы.\n- [ ] Через меню **VK Auction -> Настройка авторизации (VK)** установлены `VK_TOKEN`, `GROUP_ID`, `CONFIRMATION_CODE`, `VK_SECRET` (а также `PAYMENT_PHONE`, `PAYMENT_BANK`, `ADMIN_PASSWORD`).\n- [ ] В листе **Настройки** (`Settings`) заполнены все необходимые параметры (`max_bid`, `bid_step`, `delivery_rules` и т.д.).\n- [ ] Callback API настроен в группе VK, URL веб-приложения указан, события включены (`wall_post_new`, `wall_reply_new`, `message_new`).\n- [ ] Все необходимые триггеры (`processEventQueue`, `processNotificationQueue`, `finalizeAuction`) активны (проверить в редакторе Apps Script -> Триггеры).\n- [ ] Проведено тестирование с использованием симулятора (см. Шаг 6).\n- [ ] Клиент обучен работе с таблицей и симулятором (видео или созвон).\n- [ ] Инструкция передана клиенту (PDF или Google Docs).\n\n---\n\n## 10. КРИТЕРИИ ПРИЁМКИ (АКТУАЛЬНО)\n\n| ID | Тест | Ожидаемый результат | Метрика |\n|----|------|---------------------|---------|\n| **T-001** | Пост с `#аукцион` опубликован | Добавлен в \"Лоты\" (`Config`) за < 2 сек | ✅ Время < 2 сек (ответ VK) |\n| **T-002** | Валидная ставка \"500\" в комментарии | Записана в \"Ставки\" (`Bids`), обновлен \"Лоты\" (`Config`) за < 2 сек (ответ VK) | ✅ Время < 2 сек (ответ VK) |\n| **T-003** | Ставка 400 < текущей 500 | Отклоняется, пользователь получает ЛС \"Твоя ставка ниже текущей\" | ✅ Валидация, Уведомление |\n| **T-004** | Ставка 1000001 > `max_bid` (например, 1М) | Отклоняется | ✅ Валидация |\n| **T-005** | Ставка не кратна шагу (например, `Старт 100, Шаг 50`, ставка `175`) | Отклоняется | ✅ Валидация |\n| **T-006** | Две одновременные валидные ставки от разных пользователей | Обе записаны в \"Ставки\" (`Bids`), \"Лоты\" (`Config`) отражает только последнюю (более высокую или более позднюю), предыдущий лидер получает \"Ваша ставка перебита\" | ✅ Lock работает, Уведомления |\n| **T-007** | Уведомление перебитого | ЛС пришло за < 3 минуты | ✅ Асинхронная очередь |\n| **T-008** | Завершение в 21:00 | 15 победителей за < 5 мин | ✅ Время < 5 мин |\n| **T-009** | Команда \"АУКЦИОН\" в ЛС | Сводка с корректными суммами и доставкой за < 3 сек | ✅ Расчёт доставки |\n| **T-010** | Ставка за 5 минут до дедлайна | Дедлайн лота продлевается на 10 минут | ✅ Логика продления |\n| **T-011** | Имитация работы симулятора | Посты и комментарии генерируются, данные записываются в листы, уведомления ставятся в очередь | ✅ Работа симулятора |\n| **T-012** | 30 одновременных ставок | Все обрабатываются корректно, нет потери данных | ✅ Concurrent < 30 |\n| **T-013** | VK не отвечает (timeout) | Событие в `EventQueue` не теряется, повторная обработка (если `processEventQueue` не отработает с ошибкой) | ✅ Отказоустойчивость |\n\n---\n\n## 11. СРОКИ И ОЦЕНКА (АКТУАЛЬНО)\n\n### 11.1 Разбивка по времени\n\n| Этап | Задачи | Часы |\n|------|--------|------|\n| **Настройка инфраструктуры** | Таблица, Apps Script, PropertiesService | 2 |\n| **doPost + EventQueue** | Асинхронная обработка событий, confirmation | 4 (увеличено из-за EventQueue) |\n| **PostTracker** | Автопоиск постов, парсинг (с учетом шага, дедлайна) | 3 (увеличено) |\n| **BidProcessor** | Обработка ставок, Lock, валидация (с учетом `max_bid`, уведомлений) | 6 (увеличено) |\n| **NotificationQueue** | Очередь уведомлений, debounce, rate limiting (с учетом `low_bid`) | 4 (увеличено) |\
| **WinnerManager** | Определение победителей, триггер | 3 |\n| **ReportGenerator** | Команда АУКЦИОН, расчёт доставки | 2 |\n| **AuctionSimulator** | Разработка симулятора, сценарии, многопользовательские ставки | 6 (НОВАЯ) |\
| **Тестирование** | Unit-тесты, интеграционные тесты (с симулятором) | 6 (увеличено) |\
| **Отладка на боевом аукционе** | Мониторинг, исправление багов | 4 |\
| **Документация + обучение** | Инструкция, ТЗ, README, CHANGELOG, видео, передача | 8 (увеличено) |\
| **ИТОГО** | | **~48 часов** |\n\n### 11.2 Календарные сроки\n\n- **Разработка:** 6-7 дней чистой работы (48 ч / 7-8 ч/день)\n- **Тестирование:** 1-2 дня (с использованием симулятора, реальные VK тесты)\n- **Доработки:** 1-2 дня (по результатам)\n- **Обучение:** 1 день\n\n**Общий срок:** 10-12 дней календарных\n\n---\n\n## 12. СТОИМОСТЬ СОДЕРЖАНИЯ (АКТУАЛЬНО)\n\n| Ресурс | Лимит бесплатного tier | Наша нагрузка | Стоимость |\n|--------|------------------------|---------------|-----------|\n| **Google Apps Script** | 20,000 URL Fetch/день, 90 мин runtime/день | ~500 URL Fetch/день, ~10 мин runtime/день | **0₽/мес** |\n| **Google Sheets** | 15 ГБ | ~1 МБ/год | **0₽/мес** |\
| **VK API** | Без ограничений (бесплатно) | ~500 запросов/день | **0₽/мес** |\
| **ИТОГО** | — | — | **0₽/мес** |\n\n**Прогноз на 3 года:** 0₽ (все компоненты бесплатны навсегда для данной нагрузки)\n\n---\n\n## 13. РИСКИ И МИТИГАЦИЯ (АКТУАЛЬНО)\n\n| Риск | Вероятность | Влияние | Митигация |\n|------|-------------|---------|-----------|\n| **VK изменит Callback API / API методы** | Средняя | Высокое | Мониторинг changelog VK, быстрая адаптация (1-2 часа), гибкая функция `callVk` |\n| **Google сделает GAS платным / изменит квоты** | Низкая | Среднее | Оптимизация кода, миграция на альтернативную платформу (например, Vercel/Railway для Node.js) |\n| **Превышение лимитов GAS** | Низкая | Среднее | Оптимизация кода, батчинг запросов, асинхронная архитектура, мониторинг `Статистики` |\n| **Блокировка бота ВК** | Низкая | Критическое | Compliance с ToS VK, использование официального API, аккуратная работа с токенами |\n| **Race conditions (`LockService` fails)** | Низкая | Высокое | `LockService` с таймаутом `5` сек, повторный запрос данных внутри `Lock`, логирование, `Monitoring.recordEvent` |\n| **Скоп расползается (feature creep)** | Средняя | Среднее | Чёткое ТЗ, `change requests` только после MVP. **Текущая версия закрывает базовый функционал + ключевые улучшения.** |\
| **Некорректный парсинг постов/ставок** | Средняя | Высокое | Регулярные выражения протестированы, **симулятор генерирует сложные посты для проверки**, логирование `LOT_PARSE_FAILED` |\
| **Проблема с `userToken` для симулятора** | Средняя | Высокое | Использование заглушек в симуляторе, в реальной работе требуется действительный `userToken` тестового пользователя, иначе симулятор не будет постить комментарии. |\n| **Сбои триггеров** | Низкая | Среднее | Регулярная проверка активности триггеров (возможно, автоматизированная с оповещением админа) |\n\n---\n\n## 14. ДОПОЛНИТЕЛЬНЫЕ ФУНКЦИИ (вне базового scope)\n\nСледующие функции **не входят** в текущую версию, но могут быть добавлены за дополнительную плату:\n\n| Функция | Описание | Сложность | Оценка времени |\n|---------|----------|-----------|----------------|\n| **Автонапоминания** | Через N дней после аукциона бот пишет не оформившим заказ | Средняя | +4 часа |\
| **Расширенная статистика** | Лист с графиками: популярные лоты, средний чек, конверсия. Интеграция с Google Data Studio. | Средняя | +10 часов |\
| **Множественные группы** | Поддержка нескольких групп в одной таблице (сложная модификация логики работы с листами) | Высокая | +20 часов |\
| **CRM интеграция** | Выгрузка победителей в Битрикс24/amoCRM (через API) | Высокая | +15 часов |\
| **Веб-админка** | Красивая панель управления вместо Sheets (с использованием GAS Web App + HTML/CSS/JS) | Очень высокая | +40 часов |\
| **Онлайн-эквайринг** | Интеграция с платежными системами (ЮKassa/Robokassa) | Очень высокая | +30 часов |\
| **Проверка подписки на группу** | Отклонение ставок от неподписчиков | Низкая | +2 часа |\
| **Редактирование/удаление ставок** | Обработка `wall_reply_edit` / `wall_reply_delete` для изменения или отмены ставок | Средняя | +8 часов |\n\n---\n\n## ПРИЛОЖЕНИЯ\n\n### Приложение А: Структура кода (модули) (АКТУАЛЬНО)\n\n```\nproject/\n├── Code.gs              # Основная логика: doPost(), routeEvent(), handleWallPostNew(), handleWallReplyNew(), finalizeAuction(), onOpen(), helper UI functions\n├── Sheets.gs            # Взаимодействие с Google Sheets: SHEETS config, getSheet(), appendRow(), updateRow(), getSheetData(), log functions\n├── VkApi.gs             # Оболочка для VK API: callVk() with rate limiting/error handling\n├── Monitoring.gs        # Независимый логгер событий: Monitoring.recordEvent()\n├── AuctionSimulator.gs  # Симулятор аукциона: runSingleSimulation(), setupHourlySimulation(), stopSimulation(), resetSimulationCounter(), chooseBidScenario(), postCommentAsUser()\n├── appsscript.json      # Манифест проекта Apps Script (разрешения, триггеры)\n├── AuthSettings.html    # UI для настройки авторизации\n├── Instructions.html    # UI для отображения инструкции\n├── Login.html           # UI для входа (пароль админа)\n├── CHANGELOG.md         # Журнал изменений\n├── README.md            # Основная документация проекта\n├── vk-auction-tech-spec-v2.md # Техническая спецификация\n├── FULL_PROJECT_SPEC.md # Полная спецификация проекта (данный документ)\n└── ... прочие файлы (например, .claspignore, .clasprc.json, .gitignore, package.json, package-lock.json, debug_vk.py, DEPLOYMENT.md, DOC_VK_GAS_INTEGRATION.md, смотри. мы будем это публиковать на гитхаб.pdf)\n```\n\n### Приложение Б: Примеры VK событий (АКТУАЛЬНО)\n\n**wall_post_new (упрощенный пример):**\n```json\n{\n  \"type\": \"wall_post_new\",\n  \"object\": {\n    \"id\": 48965,\n    \"from_id\": -202389657,\n    \"owner_id\": -202389657,\n    \"date\": 1707206400,\n    \"text\": \"#аукцион@dndpotustoronu №1840\\n...подробное описание лота как в примере...\\n👀Старт 150р и шаг - 50р.\\nДедлайн 13.12.2025 в 21:00 по МСК!\",\n    \"post_type\": \"post\"\n  },\n  \"group_id\": 202389657\n}\n```\n\n**wall_reply_new (упрощенный пример):**\n```json\n{\n  \"type\": \"wall_reply_new\",\n  \"object\": {\n    \"id\": 777,\n    \"from_id\": 12345, // ID тестового пользователя\n    \"date\": 1707208800,\n    \"text\": \"500\", // Ставка\n    \"post_owner_id\": -202389657,\n    \"post_id\": 48965\n  },\n  \"group_id\": 202389657\n}\n```\n\n---\n\n**Конец технического задания**\n\n_Версия 2.1 от 10.02.2026_  \n_Учтены: Асинхронная архитектура, EventQueue, NotificationQueue, LockService, Rate Limiting, `max_bid`, продление аукциона, полный журнал ставок, уведомления о низких ставках, AuctionSimulator._\n
---

## Приложение C: Детализированровые требования из переписки (от 11.02.2026)

> **Статус:** Требования из этого раздела приняты в работу 11.02.2026. Идет реализация [диалога с пользователем в ЛС] и [новой схемы хранения данных].


Этот раздел структурирует и суммирует требования, предоставленные пользователем.

### 1. Расписание и шаблоны постов

-   **Частота:** Каждую пятницу.
-   **Количество:** 15 постов с лотами + 1 пост с подарком.

#### 1.1 Шаблон поста с лотом

```
#аукцион@dndpotustoronu №1840
При поддержке GABRIGAME-WORKSHOP!
Дедлайн 13.12.2025 в 21:00 по МСК!
🎁Лот - на картинке. + миниатюра идет с красивой, текстурной базой.

👀Старт 200р и шаг - 50р.
Каждая миниатюра аукциона масштабом 32-35мм.
ПОДАРОК ТОМУ, КТО ЗАБЕРЁТ ЗА ДЕНЬ БОЛЬШЕ ВСЕГО МИНИАТЮР!
Дата окончания аукциона 13.12.2025 (суббота) в 21:00 по Москве.

В случае, если за 10 минут (или меньше) до окончания аукциона делается ставка, например, в 20:59, аукцион на данный лот продлевается на 10 минут - до 21:09. Начиная с 20:50, продление на 10 минут происходит с каждой новой ставкой.

После аукциона пиши ТОЛЬКО в ЛС группы. Опасайся МОШЕННИКОВ пишущих тебе в ЛС. Отправь Телефон, ФИО, Город, Адрес (пункт СДЭК). И как тебе отправить, Почтой или СДЭКом.

ДОСТАВКА ЗА СЧЁТ ПОБЕДИТЕЛЯ почтой России с отправкой из Волгограда. (До 3 фигурок 550р, дальше уточним). Отправка по четвергам.
Оплата на карту в течение 3 дней после победы.
```

#### 1.2 Шаблон поста с подарком

```
А вот и ПОДАРОК самому активному игроку на аукционе СЕГОДНЯ!
Чтобы получить миниатюру, выиграй БОЛЬШЕ ВСЕХ миниатюр!
Миниатюрка обработана и загрунтована под покраску
Материал - смола
П.С. Если 2 человека выиграют одинаковое количество миниатюр, подарок переносится на следующий аукцион.
```

### 2. Функциональные требования к боту

#### 2.1 Взаимодействие в комментариях

1.  **При перебитии ставки:** Бот должен написать в комментариях к ставке, которую перебили:
    > Ваша ставка перебита

2.  **При победе в аукционе:** После завершения таймера по лоту, бот должен ответить на комментарий победителя:
    > Поздравляем с победой в аукционе за миниатюрю! Напишите в сообщения группы "Аукцион (дата)", чтобы забрать свой лот

#### 2.2 Взаимодействие в ЛС группы (по команде)

Когда пользователь-победитель пишет в ЛС группы, бот должен:

1.  **Выдать сводку по выигранным лотам** за текущий аукцион:
    -   Картинка лота
    -   Номер лота
    -   Цена, за которую лот был выигран

2.  **Запросить данные для отправки**, предоставив шаблон:
    > Если да, то пришлите пожалуйста общую сумму за все фигурки. И ВСЕ данные для отправки (Город, адрес, ФИО и номер телефона.) Можно указать ПОЧТА или СДЭК (указав адрес СДЕКа).

3.  **Предоставить реквизиты для оплаты**, включая стоимость доставки:
    > Также скрин оплаты на номер:
    > +79110702193
    > Юрий Т.
    > СБЕР
    >
    > + за отправку фигурок
    > 1-3 миниатюры (550₽)
    > 4-6 миниатюр (650₽)
    > 7+ миниатюр (750₽)

4.  **Задать вопрос о накоплении:**
    > П.С. Вы можете копить миниатюры, и перевести только за них. Аукцион каждую субботу. Накопите, и отправим все сразу.
    >
    > Будете копить?

#### 2.3 Отчет для администраторов

-   **Когда:** В момент окончания всех лотов.
-   **Куда:** Отправляется в ЛС администраторам (ID которых задаются в настройках).
-   **Формат:** Сводка по каждому победителю.
    ```
    [Имя победителя 1]
    - "картинка" лота - номер лота - цена ставки.
    - "картинка" лота - номер лота - цена ставки.
    [Имя победителя 2]
    - "картинка" лота - номер лота - цена ставки.
    ```

### 3. Дополнительные (желательные) функции

1.  **(РЕАЛИЗОВАНО)** Данные для отправки, которые пользователи пишут в ЛС, должны автоматически собираться в отдельную таблицу Google Sheets.

---


### 4. Реализация и автоматизация (от 11.02.2026)

Этот раздел описывает, как были реализованы требования из п.2 и п.3, и подтверждает их автоматическое создание.

-   **Новая структура данных (`Пользователи`, `Заказы`):**
    -   Вместо листа `Победители` были созданы два новых листа:
        -   `Пользователи`: Для ведения истории по каждому клиенту.
        -   `Заказы`: Для детального учета каждого выигранного лота.
    -   **Автоматизация:** Оба этих листа, включая все необходимые заголовки, **создаются автоматически** при запуске функции "Мастер настройки" (`runSetupWizard`).

-   **Автоматический диалог в ЛС:**
    -   Реализована логика в функции `handleMessageNew`, которая обрабатывает личные сообщения боту.
    -   **Автоматизация:** Бот реагирует на кодовое слово (по умолчанию "Аукцион"). Это слово является настраиваемой переменной `CODE_WORD` в листе `Настройки` и **создается автоматически** при первом запуске "Мастера настройки".

-   **Отчеты для администраторов:**
    -   Отчет для администраторов теперь включает не картинку-вложение, а **прямые ссылки на пост VK** для каждого выигранного лота, как и было уточнено.

-   **Парсинг изображений:**
    -   Система теперь автоматически извлекает из поста с лотом `attachment_id` и `image_url` и сохраняет их в лист `Лоты`.

---

## Приложение D: Полные циклы симуляции (с детализацией и чек-листами)

Этот раздел описывает расширенные симулированные циклы действий бота и пользователя, а также конкретные изменения в Google Sheets, которые должны произойти на каждом этапе.

#### **1. Полный цикл действий Бота (Симуляция)**

Симулятор выполняет всю работу бота от создания лота до отправки отчетов администратору.

**Шаги цикла:**

1.  **Публикация лота:**
    *   **Действие:** Симулятор создает новый пост с лотом на стене группы VK.
    *   **Ожидаемые изменения в таблице:**
        *   **Лист `Лоты`:** Появляется новая строка для этого лота со статусом `active`, полями `lot_id`, `post_id`, `name`, `start_price`, `current_price`, `deadline`, `image_url`, `attachment_id`.
        *   **Лист `Статистика`:** Записывается событие `LOT_CREATED`.
    *   **Чек-лист проверки:**
        *   [ ] Пост создан на стене VK (визуально или через API).
        *   [ ] В листе `Лоты` появилась новая строка для симулированного лота.

2.  **Эмуляция торгов (несколько ставок):**
    *   **Действие:** Симулятор от имени тестового пользователя (USER_TOKEN) отправляет несколько комментариев-ставок к посту с лотом, используя `postCommentAsUser()`. Включаются сценарии валидных, заниженных и неверных ставок.
    *   **Ожидаемые изменения в таблице:**
        *   **Лист `Ставки`:** Для каждой ставки появляется новая строка с `bid_id`, `lot_id`, `user_id`, `bid_amount`, `timestamp`, `comment_id`, `status`. Статус последней валидной ставки — `лидер`.
        *   **Лист `Лоты`:** Обновляются поля `current_price` и `leader_id` у данного лота.
        *   **Лист `Статистика`:** Записываются события `BID_PLACED` или `BID_INVALID`.
    *   **Чек-лист проверки:**
        *   [ ] В листе `Ставки` есть записи о всех симулированных ставках для этого лота.
        *   [ ] В листе `Лоты` `current_price` и `leader_id` соответствуют последней валидной ставке.

3.  **Завершение аукциона:**
    *   **Действие:** Симулятор устанавливает короткий дедлайн для лота (например, через 2-3 минуты после начала). После наступления дедлайна симулятор вызывает глобальную функцию `finalizeAuction()`.
    *   **Ожидаемые изменения в таблице:**
        *   **Лист `Лоты`:** У лота статус меняется с `active` на `sold`.
        *   **Лист `Заказы`:** Появляется новая строка с `order_id`, `lot_id`, `user_id` тестового пользователя, `win_price` и статусом `unpaid`.
        *   **Лист `Пользователи`:** У тестового пользователя обновляется `last_win_date` и инкрементируется `total_lots_won`. Если пользователя не было, он создается.
        *   **Лист `Очередь` (`NotificationQueue`):** Появляется новая задача на отправку уведомления победителю (`type: winner`).
        *   **Лист `Статистика`:** Записываются события `AUCTION_FINALIZATION_STARTED`, `WINNER_DECLARED`, `ADMIN_REPORTS_SENT`.
    *   **Чек-лист проверки:**
        *   [ ] Статус лота в листе `Лоты` изменен на `sold`.
        *   [ ] В листе `Заказы` создана строка с неоплаченным заказом для победителя.
        *   [ ] В листе `Пользователи` обновлены данные победителя.
        *   [ ] В `Очереди` (`NotificationQueue`) появилось задание на уведомление победителя.
        *   [ ] Администратор получил в ЛС отдельное сообщение-сводку по этому победителю.

---
#### **2. Полный цикл действий Победителя (Симуляция)**

После того как цикл бота завершился, симулятор начинает действовать от имени победившего тестового пользователя. Для этого необходимо использовать `USER_TOKEN` тестового пользователя.

**Шаги цикла:**

1.  **Запрос сводки по заказам:**
    *   **Действие:** Симулятор от имени тестового пользователя отправляет в ЛС группы кодовое слово (`CODE_WORD` из настроек).
    *   **Ожидаемое действие бота:** Бот присылает ответное сообщение со сводкой по заказу.
    *   **Чек-лист проверки:**
        *   [ ] Бот прислал в ЛС сообщение-сводку с расчетом стоимости (по наличию ключевых слов, например, "ИТОГО К ОПЛАТЕ:").

2.  **Отправка данных для доставки:**
    *   **Действие:** Симулятор отправляет в ЛС группы сообщение с тестовыми ФИО, адресом и телефоном.
    *   **Ожидаемые изменения в таблице:**
        *   **Лист `Пользователи`:** В строке тестового пользователя в колонке `shipping_details` должны появиться распознанные данные.
        *   **Лист `Статистика`:** Записывается событие `SHIPPING_INFO_RECEIVED`.
    *   **Чек-лист проверки:**
        *   [ ] В листе `Пользователи` в строке тестового пользователя заполнилась ячейка `shipping_details`.

3.  **Проверка подтверждения:**
    *   **Действие:** Симулятор проверяет, что бот в ответ прислал сообщение-подтверждение о приеме данных.
    *   **Ожидаемое действие бота:** Бот присылает сообщение "✅ Спасибо, данные для доставки приняты!".
    *   **Чек-лист проверки:**
        *   [ ] Бот прислал в ЛС сообщение "✅ Спасибо, данные для доставки приняты!".

---

### **Задание для ИИ Агента:**

На основе описанных выше полных циклов симуляции, модифицируй файл `auction/AuctionSimulator.gs`. Создай новую функцию `runFullCycleSimulation()`, которая будет последовательно выполнять все шаги циклов Бота и Победителя. На каждом шаге должна производиться самопроверка с чтением данных из Google Sheets, чтобы убедиться в корректности работы предыдущего этапа. В случае ошибки на любом этапе симуляция должна останавливаться с подробным логом.

Убедись, что `runFullCycleSimulation()` будет использовать существующие функции (`postCommentAsUser`, `callVk`, `getSheetData`, `updateLot` и т.д.) и адекватно обрабатывать задержки (`Utilities.sleep()`) между действиями, чтобы имитировать реальное взаимодействие с VK API и дать время на обработку событий.
