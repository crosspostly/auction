# Журнал Изменений (CHANGELOG)

## Версия [Будет определено позже] - [Текущая дата]

### Новые Функции и Улучшения

*   **Полная запись всех ставок:**
    *   **`Code.gs`**: Теперь все валидные ставки, сделанные в комментариях, записываются в лист **"Ставки"** (`Bids`) со всеми деталями: `bid_id`, `lot_id`, `user_id`, `bid_amount`, `timestamp` и `comment_id`. Это обеспечивает полную историю торгов по каждому лоту.
    *   **`Monitoring.gs`**: Добавлено событие `BID_RECORDED` для отслеживания каждой записанной ставки.

*   **Уведомление о низкой ставке:**
    *   **`Code.gs`**: Реализована функция отправки личного сообщения пользователю VK, если его ставка ниже текущей цены лота или стартовой цены.
    *   **`Code.gs`**: Добавлена функция `buildLowBidMessage` для формирования дружелюбного и информативного сообщения о низкой ставке.
    *   **`Code.gs`**: Функция `sendNotification` была обновлена для обработки нового типа уведомлений (`low_bid`).
    *   **`Monitoring.gs`**: Добавлено событие `LOW_BID_NOTIFICATION_QUEUED` для отслеживания постановки в очередь таких уведомлений.

*   **Дружелюбный тон уведомлений:**
    *   **`Code.gs`**: Сообщение о низкой ставке изменено на более дружелюбное: "👋 Привет! Твоя ставка [сумма]₽ по лоту «[название]» чуть ниже текущей цены [сумма]₽. Попробуй предложить больше, чтобы побороться за лот! 😉".

*   **Логика продления аукциона:**
    *   **`Code.gs`**: Реализован механизм автоматического продления дедлайна лота. Если ставка делается в пределах 10 минут до текущего дедлайна, аукцион продлевается на 10 минут.
    *   **`Monitoring.gs`**: Добавлено событие `AUCTION_EXTENDED` для логирования случаев продления аукциона.

*   **Валидация по максимальной ставке (`max_bid`):**
    *   **`Code.gs`**: В функцию `validateBid` добавлена проверка, отклоняющая ставки, которые превышают установленный в настройках `max_bid`.

*   **Улучшенная симуляция ставок от разных пользователей:**
    *   **`AuctionSimulator.gs`**: Удалены одиночные `userToken` и `testUserId`.
    *   **`AuctionSimulator.gs`**: Добавлен массив `SIMULATOR_SETTINGS.testUsers`, содержащий несколько заглушек `user_id` и `userToken` для имитации ставок от разных пользователей.
    *   **`AuctionSimulator.gs`**: `runSingleSimulation` теперь случайным образом выбирает тестового пользователя для каждой имитируемой ставки.
    *   **`AuctionSimulator.gs`**: Функция `postCommentAsUser` обновлена для приема и использования токена выбранного пользователя.
    *   **`Monitoring.gs`**: В логи симулятора теперь включается `userId` для каждой опубликованной ставки.

### Обновления Симулятора Аукциона

*   **Реалистичные шаблоны постов:**
    *   **`AuctionSimulator.gs`**: Шаблон для создания постов-лотов теперь соответствует подробному примеру пользователя, включая `Дедлайн`, `Старт` и `Шаг`, а также другие детали.
*   **Улучшенные сценарии ставок:**
    *   **`AuctionSimulator.gs`**: Функция `chooseBidScenario` была изменена для более частого включения сценариев `LOWER_BID` и `INVALID_STEP`, что обеспечивает более тщательное тестирование новых функций уведомлений и валидации.
    *   **`AuctionSimulator.gs`**: Логика обновления `currentBid` в симуляторе теперь учитывает только потенциально валидные ставки, что делает симуляцию более точной.

### Технические изменения и рефакторинг

*   **`Code.gs`**: `parseLotFromPost` теперь более надежно извлекает `bid_step` и `deadline` из текста поста, обрабатывая различные форматы.
*   **`Code.gs`**: `handleWallPostNew` теперь использует извлеченный `bid_step` при создании данных нового лота.
*   **`Sheets.gs`**: Добавлен новый столбец `bid_step` в определение листа `Config` для сохранения шага ставки каждого лота.

## Предыдущие изменения

### 1. Независимая Система Мониторинга

-   **`Sheets.gs`**: Добавлено новое определение листа для `Статистика` (`Statistics`). Этот лист будет хранить все ключевые события аукциона для анализа.
-   **`Monitoring.gs`**: Создан новый, независимый файл скрипта.
    -   Содержит единственную функцию `Monitoring.recordEvent(eventType, data)`, единственной обязанностью которой является запись событий в лист `Статистика`.
    -   Это гарантирует, что мониторинг decoupled от основной логики приложения.

### 2. Интеграция Мониторинга

-   **`Code.gs`**: Интегрированы вызовы `Monitoring.recordEvent` во все критические точки жизненного цикла аукциона:
    -   `LOT_CREATED`: Когда новый лот парсится из поста VK.
    -   `LOT_PARSE_FAILED`: Если пост с хештегом `#аукцион` не может быть распарсен.
    -   `BID_RECEIVED`: Когда получен новый комментарий к посту лота.
    -   `BID_VALIDATED`: После проверки ставки на валидность, логируется результат (успех или неудача с причиной).
    -   `LEADER_UPDATED`: Когда валидная ставка делает пользователя новым лидером.
    -   `OUTBID_NOTIFICATION_QUEUED`: Когда уведомление ставится в очередь для пользователя, ставка которого перебита.
    -   `AUCTION_FINALIZATION_STARTED`: Когда начинается процесс `finalizeAuction`.
    -   `WINNER_DECLARED`: Когда лот успешно продан победителю.
    -   `LOT_UNSOLD`: Когда лот завершается без ставок.
-   **`Code.gs`**: Обновлена функция `parseBid` для более гибкого парсинга ставок, корректно извлекая суммы ставок, даже если это просто числа без конкретных символов валюты.
-   **`Code.gs`**: Добавлена запись `logInfo` в `handleWallReplyNew` для записи комментариев, которые не распознаны как ставки, обеспечивая лучшую видимость нераспаршенных комментариев.
-   Функция `validateBid` была улучшена для возврата подробного объекта с причиной неудачи, которая теперь логируется.

### 3. Симулятор Аукциона

-   **`AuctionSimulator.gs`**: Создан новый файл скрипта для размещения всей логики симуляции.
    -   **`runSingleSimulation()`**: Основная функция, которая имитирует один полный цикл аукциона:
        -   Публикует **реальный пост** на стене группы VK с уникальным тестовым ID лота.
        -   Публикует 10-30 **реальных комментариев** под постом, используя предоставленный токен пользователя, имитируя различные сценарии ставок.
    -   **Реализованные сценарии ставок**:
        -   **Valid Bids**: Стандартные инкрементные ставки.
        -   **High Frequency**: Множественные ставки, отправленные с очень короткими задержками для проверки обработки параллелизма.
        -   **Same Bid**: Ставка, равная текущей цене.
        -   **Lower Bid**: Ставка ниже текущей цены.
        -   **Invalid Step**: Ставка, которая не является кратной требуемому шагу.
    -   **Элементы управления симуляцией**:
        -   `setupHourlySimulation()`: Устанавливает временной триггер для запуска симуляции раз в час.
        -   `stopSimulation()`: Удаляет триггер для остановки симуляции.
        -   `resetSimulationCounter()`: Сбрасывает счетчик постов (максимум 5 постов за полный цикл симуляции).
-   **`VkApi.gs`**: Функция `callVk` была немного изменена для необязательного приема пользовательского токена, что позволяет симулятору публиковать комментарии от имени пользователя.

### 4. Пользовательский Интерфейс

-   **`Code.gs`**: Добавлено новое подменю "СИМУЛЯТОР" в главное меню "VK Auction" в таблице, обеспечивающее легкий доступ к:
    -   Запуску одного цикла симуляции вручную.
    -   Включению/отключению ежечасной симуляции.
    -   Сбросу счетчика постов.

## Как использовать

1.  Откройте Google Таблицу.
2.  Используйте меню **VK Auction > СИМУЛЯТОР** для управления симуляцией.
3.  Наблюдайте за появлением новых постов и комментариев в группе VK.
4.  Просмотрите лист **`Статистика`**, чтобы увидеть журнал всех событий, генерируемых симуляцией.
