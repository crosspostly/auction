# Руководство по развертыванию VK Auction Bot

## Быстрый старт

### Шаг 1: Создание проекта Google Apps Script

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу с названием "VK Auction Bot"
3. Перейдите в **Расширения** → **Apps Script**
4. Удалите стандартный код
5. Создайте следующие файлы:

#### Создание файлов в Apps Script

Нажмите **+** рядом с "Файлы" для каждого:

- `Code.gs` - скопируйте содержимое из репозитория
- `Sheets.gs` - скопируйте содержимое из репозитория
- `Vk.gs` - скопируйте содержимое из репозитория
- `Tests.gs` - скопируйте содержимое из репозитория (опционально)

6. Откройте файл `appsscript.json` в редакторе (значок ⚙️ → "Показать файл манифеста appsscript.json")
7. Замените содержимое на:

```json
{
  "timeZone": "Europe/Moscow",
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8"
}
```

### Шаг 2: Получение VK Access Token

1. Перейдите на [VK Host](https://vkhost.github.io)
2. Выберите **VK Admin**
3. Авторизуйтесь через ВКонтакте
4. Выберите права доступа:
   - ✅ messages (работа с личными сообщениями)
   - ✅ wall (управление записями на стене)
   - ✅ groups (управление сообществом)
5. Скопируйте `access_token` из адресной строки после `#access_token=`

**Важно**: Токен начинается с `vk1.a.` и имеет длину около 85 символов.

### Шаг 3: Настройка VK Callback API

1. Откройте настройки вашего сообщества ВКонтакте
2. Перейдите в **Управление** → **Работа с API** → **Callback API**
3. Нажмите **Создать сервер**
4. Заголовок: "Auction Bot"
5. **Не заполняйте URL пока** - вернёмся к этому в Шаге 5
6. Включите события:
   - ✅ Комментарии к записям (`wall_reply_new`)
   - ✅ Новая запись на стене (`wall_post_new`)
   - ✅ Входящее личное сообщение (`message_new`)
7. Скопируйте:
   - **Secret key** (секретный ключ)
   - **Строку для подтверждения** (confirmation string) - появится после первого запроса

### Шаг 4: Первоначальная настройка в Google Sheets

1. Вернитесь в вашу Google Sheets таблицу
2. Обновите страницу (F5)
3. В меню появится новый пункт **VK Auction**
4. Выберите **VK Auction** → **Создать/Обновить листы**
   - Будут созданы листы: Лоты, Settings, Bids, Winners, Shipping, Logs, Errors, Queue
5. Выберите **VK Auction** → **Настройка авторизации (VK)**
6. Введите следующие данные:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `VK_TOKEN` | Access token из Шага 2 | `vk1.a.xxxxx...` |
| `GROUP_ID` | ID вашего сообщества **без минуса** | `123456789` |
| `CONFIRMATION_STRING` | Строка подтверждения из Callback API | `abc12345` |
| `VK_SECRET` | Secret key из Callback API | `secret123` |
| `PAYMENT_PHONE` | Телефон для оплаты (СБП) | `+79001234567` |
| `PAYMENT_BANK` | Название банка | `Сбербанк` |
| `DELIVERY_RULES` | Правила доставки | `1-3:300, 4-6:500, 7+:0` |
| `ADMIN_IDS` | ID администраторов через запятую | `12345,67890` |

**Формат DELIVERY_RULES**:
- `1-3:300` - от 1 до 3 лотов: 300 руб.
- `4-6:500` - от 4 до 6 лотов: 500 руб.
- `7+:0` - 7 и более лотов: бесплатно

**Как узнать GROUP_ID**:
1. Откройте страницу вашего сообщества
2. ID в адресной строке: `vk.com/club123456789` → `123456789`
3. Или используйте сервис [RegVK](https://regvk.com/id/)

### Шаг 5: Публикация веб-приложения

1. В редакторе Apps Script нажмите **Развернуть** → **Управление развертываниями**
2. Выберите существующее развертывание с ID: `AKfycbz5QY9W2VUdF_xdafv_DH6yDpdPEN1zsEUF6wSEqOQvwueBgMWWAsze4fmNRFEJkryY`
3. Нажмите иконку карандаша ✏️ (Редактировать)
4. Выберите **Новая версия**
5. Нажмите **Развернуть**

**ВАЖНО**: Для этого проекта используется фиксированный URL, к которому привязан прокси:
`https://script.google.com/macros/s/AKfycbz5QY9W2VUdF_xdafv_DH6yDpdPEN1zsEUF6wSEqOQvwueBgMWWAsze4fmNRFEJkryY/exec`

При использовании `clasp` всегда деплойте в этот ID:
```bash
npx clasp deploy -i AKfycbz5QY9W2VUdF_xdafv_DH6yDpdPEN1zsEUF6wSEqOQvwueBgMWWAsze4fmNRFEJkryY -d "Описание версии"
```

### Шаг 6: Завершение настройки Callback API

1. Вернитесь в настройки Callback API в VK
2. Вставьте скопированный URL в поле **Адрес**
3. Нажмите **Подтвердить**
   - VK отправит запрос на ваш сервер
   - Бот должен ответить строкой подтверждения
   - Статус должен измениться на ✅ **Подтверждён**

### Шаг 7: Настройка триггеров

1. В Google Sheets выберите **VK Auction** → **Настроить триггеры**
2. Подтвердите разрешения:
   - Нажмите **Разрешить доступ**
   - Выберите ваш Google аккаунт
   - Нажмите **Дополнительно** → **Перейти на страницу "VK Auction Bot"**
   - Нажмите **Разрешить**
3. Триггеры будут настроены автоматически:
   - Обработка очереди: каждую минуту
   - Health check: каждый час

### Шаг 8: Проверка работоспособности

1. Выберите **VK Auction** → **Проверить соединение**
   - Должно появиться сообщение с названием вашего сообщества
2. Выберите **VK Auction** → **Статус**
   - Проверьте количество лотов, ставок, очереди

## Автоматический Деплой и Тестирование (CI/CD)

В проект встроен механизм для автоматического деплоя кода и запуска полной симуляции аукциона. Это позволяет проверять работоспособность бота сразу после внесения изменений.

### Компоненты
- `deploy_and_test.sh`: Скрипт оболочки, который выполняет `clasp push` и вызывает Web App URL для запуска тестов.
- `Code.gs` (`doGet`): Обработчик, который по параметру `?action=run_tests` запускает функцию `runFullCycleSimulation`.

### Настройка Debug Mode
1. Убедитесь, что у вас установлен `WEB_APP_URL` и `VK_SECRET` как переменные окружения или прописаны внутри `deploy_and_test.sh`.
2. Доступ к веб-приложению должен быть **"Все" (Anyone)**, чтобы скрипт мог выполнить запрос без ручной авторизации в браузере.
   - *Примечание:* Для работы VK Webhooks доступ "Все" и так является обязательным.

### ⚠️ Важно: Доступ "Все" (Anyone)

Даже если в `appsscript.json` указано `"access": "ANYONE"`, Google Apps Script часто сбрасывает эту настройку при создании новых версий через API или требует ручного подтверждения.

**Симптомы проблемы:**
- `curl` или скрипт `deploy_and_test.sh` возвращают HTML-код страницы входа Google вместо JSON.
- Ошибка `Exit Code: 1` в CI/CD пайплайне.

**Решение:**
1. Откройте редактор скриптов.
2. Нажмите синюю кнопку **Начать развертывание** -> **Управление развертываниями**.
3. Выберите активное развертывание (карандаш ✏️).
4. Убедитесь, что в поле "У кого есть доступ" стоит **"Все"** (не "Все, у кого есть аккаунт Google").
5. Если нет — измените и нажмите "Готово".

3. Запуск:
   ```bash
   ./deploy_and_test.sh
   ```

### Отключение тестов в Продакшене
Хотя запуск тестов защищен секретным ключом (`VK_SECRET`), вы можете захотеть полностью отключить эту возможность в рабочей версии.

**Способ 1: Комментирование кода (Рекомендуется)**
В файле `Code.gs` найдите блок обработки `run_tests` и закомментируйте его:

```javascript
function doGet(e) {
  // --- КОММЕНТИРОВАТЬ ОТСЮДА ---
  // if (e.parameter && e.parameter.action === 'run_tests') {
  //   ... логика тестов ...
  // }
  // --- ДО СЮДА ---

  return ContentService.createTextOutput("Сервер жив...");
}
```
После этого любой вызов `deploy_and_test.sh` будет возвращать стандартный ответ "Сервер жив", но тесты запускаться не будут.

**Способ 2: Смена секрета**
Если вы подозреваете, что ваш `VK_SECRET` утек, смените его в настройках сообщества VK и обновите в скрипте через меню "Настройка авторизации". Старые скрипты CI/CD перестанут работать.

## Настройка шаблонов

### Настройка листа Settings

В листе **Settings** настройте следующие параметры:

#### only_saturday

- `FALSE` - обрабатывать все посты с хэштегом `#аукцион`
- `TRUE` - обрабатывать только посты, опубликованные в субботу по МСК

#### dm_template_auction

Шаблон личного сообщения победителю. Доступные переменные:

- `{lots}` - список выигранных лотов с ценами и ссылками
- `{total}` - общая сумма за лоты (без доставки)
- `{delivery}` - стоимость доставки
- `{payment_details}` - реквизиты оплаты (телефон и банк)

**Пример шаблона**:
```
Привет! 🌸

Вы выиграли в аукционе:
{lots}

ИТОГО: {total} руб.
Доставка: {delivery} руб.

Карта для оплаты: {payment_details}

После оплаты пришлите скриншок чека.
```

## Создание первого лота

### Формат поста

Создайте пост в вашем сообществе с хэштегом `#аукцион`:

```
#аукцион

Красивая вязаная шапочка 🧶

Ручная работа, 100% шерсть мериноса.
Размер: универсальный

№1001
Стартовая цена: 1000
Шаг: 100
Дедлайн: 15.02.2026 21:00
```

**Важно**:
- Обязателен хэштег `#аукцион` (русскими буквами)
- Номер лота: `№1001` или `Лот №1001`
- Формат даты: `ДД.ММ.ГГГГ ЧЧ:ММ`
- Можно прикрепить фотографию товара

### Проверка создания лота

1. После публикации поста подождите 1-2 минуты
2. Откройте лист **Лоты** в Google Sheets
3. Должна появиться новая строка с данными лота
4. Проверьте лист **Logs** - должна быть запись "Lot stored"

### Тестовая ставка

1. Оставьте комментарий к посту с числом, например: `1200`
2. Подождите 1-2 минуты
3. Откройте лист **Bids** - должна появиться ваша ставка
4. В листе **Лоты** должна обновиться текущая цена

## Мониторинг и отладка

### Просмотр логов

- **Logs** - информационные сообщения о работе системы
- **Errors** - ошибки и исключения
- **Queue** - очередь входящих событий от VK

### Типичные проблемы

#### Лоты не создаются

1. Проверьте наличие хэштега `#аукцион`
2. Убедитесь, что формат поста правильный
3. Если `only_saturday = TRUE`, убедитесь, что сегодня суббота по МСК
4. Проверьте лист **Errors**

#### Ставки не обрабатываются

1. Проверьте, что лот существует в листе **Лоты**
2. Убедитесь, что дедлайн не истёк
3. Проверьте, что сумма ставки >= текущая цена + шаг
4. Проверьте лист **Errors**

#### Уведомления не отправляются

1. Проверьте права токена (messages)
2. Убедитесь, что у сообщества включены личные сообщения
3. Проверьте, что пользователь не заблокировал сообщество
4. Проверьте лист **Errors**

### Просмотр логов Apps Script

1. В редакторе Apps Script выберите **Выполнение**
2. Просмотрите историю выполнения функций
3. Нажмите на любое выполнение для просмотра деталей

### Тестирование

Запустите встроенные тесты:

1. **VK Auction** → **Запустить тесты**
2. Просмотрите результаты всех тестов
3. Все тесты должны пройти успешно (✓ PASSED)

## Обновление бота

### При изменении кода

1. Скопируйте новую версию файлов из репозитория
2. В редакторе Apps Script нажмите **Развернуть** → **Управление развертыванием**
3. Нажмите на иконку карандаша ✏️ рядом с текущим развертыванием
4. Выберите **Новая версия**
5. Нажмите **Развернуть**

**Важно**: URL веб-приложения не изменится, обновлять Callback API не нужно.

### При изменении настроек

Если нужно изменить чувствительные данные (токены, реквизиты):

1. **VK Auction** → **Настройка авторизации (VK)**
2. Введите новые значения
3. Старые значения будут перезаписаны

## Резервное копирование

### Создание копии

1. Откройте Google Sheets
2. **Файл** → **Создать копию**
3. Дайте название: "VK Auction Bot Backup YYYY-MM-DD"

### Восстановление из копии

1. Откройте резервную копию
2. Скопируйте все листы в новую таблицу
3. Пройдите Шаги 1-7 заново для создания нового проекта Apps Script

## Безопасность

### Защита чувствительных данных

✅ **Правильно** (используйте PropertiesService):
- Используйте меню **VK Auction** → **Настройка авторизации (VK)**
- Данные хранятся в PropertiesService (недоступны в таблице)

❌ **Неправильно**:
- НЕ записывайте токены, реквизиты, ID админов в лист Settings
- НЕ публикуйте скриншоты листа Settings с чувствительными данными

### Права доступа

Ограничьте доступ к Google Sheets:

1. Нажмите **Настройки доступа** в правом верхнем углу
2. Убедитесь, что доступ имеют только нужные пользователи
3. Установите права "Редактор" только для доверенных лиц

### Регулярные проверки

1. Еженедельно проверяйте листы **Errors** и **Logs**
2. Следите за неожиданной активностью
3. Обновляйте токены при необходимости

## Поддержка

Если у вас возникли проблемы:

1. Проверьте логи ошибок в листе **Errors**
2. Изучите документацию в README.md
3. Проверьте типичные проблемы в разделе "Мониторинг и отладка"
4. Создайте Issue в репозитории GitHub

## Дополнительные возможности

### Ручное завершение аукциона

Если нужно завершить все активные лоты досрочно:

1. **VK Auction** → **Завершить активные лоты**
2. Все лоты со статусом ACTIVE будут завершены
3. Победители получат уведомления
4. Администраторы получат сводку

### Очистка кэша событий

Если события обрабатываются некорректно:

1. **VK Auction** → **Очистить кэш**
2. Кэш дубликатов событий будет очищен

### Ручной запуск очереди

Для немедленной обработки событий из очереди:

1. **VK Auction** → **Запустить очередь**
2. Все события из листа Queue будут обработаны