# VK Auction Bot

Автоматизированный бот для проведения аукционов во ВКонтакте на базе Google Apps Script.

## Возможности

- 🔍 Автоматическое обнаружение постов-лотов по хэштегу `#аукцион`
- 💰 Отслеживание ставок в комментариях в режиме реального времени
- 📢 Уведомления участникам о перебитых ставках
- ⏰ Автоматическое продление дедлайна при ставке в последние 10 минут
- 🏆 Определение победителей и автоматическая отправка уведомлений с реквизитами
- 📊 Сводные отчёты администраторам по итогам аукциона
- 📦 Расчёт стоимости доставки по количеству лотов

## Технологии

- **Backend**: Google Apps Script (JavaScript ES6+)
- **API**: VK Callback API + VK API методы
- **Хранилище**: Google Sheets
- **Планировщик**: Time-driven triggers в GAS

## Установка

### 1. Создание Google Apps Script проекта

1. Откройте [Google Sheets](https://sheets.google.com) и создайте новую таблицу
2. Перейдите в меню **Расширения** → **Apps Script**
3. Скопируйте содержимое файлов из репозитория:
   - `Code.gs`
   - `Sheets.gs`
   - `Vk.gs`
   - `appsscript.json`

### 2. Настройка VK Callback API

1. Перейдите в настройки вашего сообщества ВКонтакте
2. Выберите раздел **Работа с API** → **Callback API**
3. Создайте новый Callback API сервер
4. URL сервера: скопируйте URL из Apps Script после публикации (Deploy → New deployment → Web app)
5. Версия API: 5.131
6. Включите следующие события:
   - `wall_post_new` (новые записи на стене)
   - `wall_reply_new` (комментарии к записям)
   - `message_new` (личные сообщения)
7. Сохраните **Secret key** и **Confirmation string**

### 3. Первоначальная настройка

1. В Google Sheets откройте меню **VK Auction**
2. Выберите **Настройка авторизации (VK)**
3. Введите следующие параметры:
   - `VK_TOKEN`: токен доступа с правами messages, wall, groups ([получить токен](https://vkhost.github.io))
   - `GROUP_ID`: ID вашего сообщества (число без минуса)
   - `CONFIRMATION_STRING`: строка подтверждения из Callback API
   - `VK_SECRET`: Secret key из Callback API
   - `PAYMENT_PHONE`: телефон для оплаты (СБП)
   - `PAYMENT_BANK`: название банка
   - `DELIVERY_RULES`: правила доставки (формат: `1-3:300, 4-6:500, 7+:0`)
   - `ADMIN_IDS`: ID администраторов через запятую (для отчётов)

4. Выберите **Создать/Обновить листы** для создания необходимых таблиц
5. Выберите **Настроить триггеры** для запуска автоматической обработки

### 4. Публикация веб-приложения

1. В редакторе Apps Script нажмите **Deploy** → **New deployment**
2. Выберите тип: **Web app**
3. Настройки:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. Скопируйте URL веб-приложения и используйте его в настройках Callback API

### 5. Настройка шаблонов

В листе **Settings** настройте:

- `only_saturday`: `TRUE` если лоты обрабатываются только по субботам, `FALSE` для всех дней
- `dm_template_auction`: шаблон сообщения победителю (доступные переменные: `{lots}`, `{total}`, `{delivery}`, `{payment_details}`)

## Формат поста-лота

```
#аукцион

Описание товара...

№1234
Стартовая цена: 1000
Шаг: 100
Дедлайн: 08.02.2026 21:00
```

**Важно:**
- Обязателен хэштег `#аукцион`
- Номер лота можно указать как `№1234` или `Лот №1234`
- Формат дедлайна: `ДД.ММ.ГГГГ ЧЧ:ММ`

## Формат ставки

Участники делают ставки в комментариях, указывая только сумму: `1500`

## Команды в личных сообщениях

- Отправьте `аукцион` боту, чтобы получить список выигранных лотов
- Отправьте адрес доставки после получения уведомления о победе

## Логика работы

1. **Обнаружение лота**: бот отслеживает новые посты с хэштегом `#аукцион` и парсит детали лота
2. **Обработка ставок**: при новых комментариях с числом бот проверяет валидность ставки и обновляет лидера
3. **Уведомления**: предыдущий лидер получает уведомление "Ваша ставка перебита" с задержкой 10-24 секунды
4. **Продление дедлайна**: если ставка сделана в последние 10 минут, дедлайн продлевается на 10 минут
5. **Финализация**: по истечению дедлайна бот отправляет победителю личное сообщение с реквизитами
6. **Сводка админам**: после завершения всех лотов администраторы получают полный отчёт

## Структура данных

### Листы Google Sheets

- **Лоты**: активные и завершённые лоты
- **Bids**: журнал всех ставок
- **Winners**: список победителей
- **Settings**: настройки шаблонов
- **Shipping**: адреса доставки от победителей
- **Queue**: очередь входящих событий от VK
- **Logs**: информационные логи
- **Errors**: журнал ошибок

## Безопасность

⚠️ **Важно**: Чувствительные данные (токены, реквизиты, ID админов) хранятся в PropertiesService и **не должны** записываться в таблицу Settings.

Используйте меню **VK Auction → Настройка авторизации** для безопасного сохранения настроек.

## Поддержка

При возникновении проблем:
1. Проверьте лист **Errors** для просмотра ошибок
2. Используйте **VK Auction → Статус** для проверки состояния системы
3. **VK Auction → Проверить соединение** для тестирования подключения к VK API

## Лицензия

MIT License
