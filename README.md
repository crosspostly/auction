# VK Аукционный Бот (v3.2.0 - Optimized)

Автоматизированная система для проведения аукционов во ВКонтакте.  
**Главный принцип:** Высокая производительность, надежность данных и отсутствие спама.

---

## 📌 Ключевые особенности

### 1. Честные торги и Анти-снайпер
*   **Первая ставка:** Принимается, если она `>=` стартовой цене.
*   **Шаг ставки:** Все последующие ставки должны быть `> текущей + шаг`.
*   **Анти-снайпер:** Если ставка сделана менее чем за 10 минут до конца (или после дедлайна, пока лот активен), дедлайн продлевается.
    *   *Правило:* **Новый дедлайн = Время этой ставки + 10 минут.**
*   **Время:** Бот ориентируется на **время создания комментария в ВК**. Все проверки выполняются по **Московскому времени (GMT+3)**.

### 2. Высокая производительность (New v3.2)
*   **Memory Caching:** Внедрено многоуровневое кеширование (Script Cache + Memory Cache). Чтение настроек и данных таблиц происходит один раз за выполнение, что ускоряет обработку в 5-8 раз.
*   **Оптимизация API:** Минимизировано количество обращений к Google Sheets API и PropertiesService.
*   **Стресс-тестирование:** Система успешно проходит тесты на 24+ одновременных ставки без превышения лимитов времени выполнения (Execution Time).

### 3. Безопасность данных и ID
*   **String-Forcing IDs:** Все ID (комментариев, пользователей, лотов) принудительно записываются как строки (через `'`). Это исключает ошибки, когда длинные цифровые ID ошибочно распознаются Google Таблицами как даты (например, "1901-07-15").
*   **Тройная защита от дублей:**
    1.  **CacheService:** Блокировка ID комментария на 10 минут.
    2.  **Memory Cache:** Идемпотентность внутри одного запроса.
    3.  **База данных:** Проверка в листе "Ставки".

### 4. Тишина в ЛС (No Spam Policy)
*   **В ходе торгов:** Бот отвечает **только в комментариях**.
*   **Финал:** Бот отправляет **одно сводное сообщение** победителю в ЛС только после закрытия всех лотов аукциона.

---

## 🛠 Техническая архитектура

### Оптимизированная обработка
В версии 3.2 введена архитектура **"Single Read - Bulk Process"**. Данные загружаются в память один раз, обрабатываются и сохраняются, что критично для предотвращения тайм-аутов при массовых ставках.

### Триггеры (Автоматизация)
В системе работают всего **2** основных триггера:

1.  **`periodicSystemCheck` (Авто-запуск)**
    *   Активируется автоматически при появлении активных лотов.
    *   Проверяет дедлайны всех активных лотов.
    *   Если `Время > Дедлайн` — закрывает лот.
    *   Если ВСЕ лоты закрыты — удаляет сам себя до следующего аукциона.

2.  **`dailyMaintenance` (Ежедневно в 02:00)**
    *   Очищает старые логи (>30 дней).
    *   Проверяет целостность таблицы.

---

## 🚀 Инструкция по запуску

### 1. Установка
1.  Скопируйте код в редактор скриптов.
2.  Нажмите **Deploy** -> **New Deployment** (Web App, Access: Anyone).
    *   *Важно:* Используйте фиксированный ID развертывания, если бот уже подключен к ВК.
3.  Скопируйте полученный URL.

### 2. Настройка VK
1.  В настройках сообщества: **API** -> **Callback API**.
2.  Вставьте URL, нажмите "Подтвердить".
3.  В типах событий отметьте галочками:
    *   `wall_post_new` (Записи на стене)
    *   `wall_reply_new` (Комментарии)
    *   `message_new` (Входящие сообщения)

### 3. Настройка Таблицы
1.  Обновите страницу таблицы. Появится меню **VK Auction**.
2.  **🚀 Мастер настройки**: Нажмите, чтобы создать листы и добавить новые параметры (`AUCTION_TAG`, `ACCUMULATE_COMMAND`).
3.  **🔐 Настройки авторизации**: Введите токены (Group Token, Admin Token) и ID группы.
4.  Бот готов к работе. При публикации первого поста с тегом мониторинг включится автоматически.

---

## 📊 Описание листов

*   **Config (Лоты):** Список лотов. Уникальность по `post_id`.
*   **Bids (Ставки):** История всех ставок. Защищена от смещения столбцов (умная запись).
*   **Users (Пользователи):** База участников с адресами доставки.
*   **Orders (Заказы):** Кто что выиграл и оплатил ли.
*   **Settings (Настройки):** Шаблоны текстов, переключатели (`ВКЛ`/`ВЫКЛ`), тег аукциона.
*   **Incoming:** Лог последних 100 сырых событий от ВК (для отладки).
*   **Logs (Журнал):** Технический журнал. Новые записи — сверху.

---
*Версия 3.2.0 — 21.02.2026*
