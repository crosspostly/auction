# VK Аукционный Бот

Автоматизированный бот для проведения аукционов во ВКонтакте на базе Google Apps Script.

## 🚀 Возможности

### Аукцион
- 🔍 **Авто-поиск лотов:** Бот сам находит посты с хэштегом `#аукцион` и создает лот в базе.
- 💰 **Ставки в реальном времени:** Отслеживание комментариев, валидация ставок (шаг, минимум, максимум).
- 📢 **Умные уведомления:**
    - "Ваша ставка перебита" (предыдущему лидеру).
    - "Ставка слишком низкая" (если меньше текущей цены).
    - "Вы выиграли" (победителю после финиша).
- ⏰ **Анти-снайпер:** Авто-продление дедлайна на 10 минут, если ставка сделана в самом конце.
- 🏆 **Финал:** Определение победителей, отправка итогов в ЛС, расчет доставки.

### Надежность и архитектура
- 🛡️ **Очереди (Queues):** Все события и уведомления проходят через очереди (`EventQueue`, `NotificationQueue`). Это гарантирует, что ничего не потеряется даже при наплыве ставок.
- 🔒 **Защита от гонок:** `LockService` предотвращает ошибки при одновременных ставках.
- 🚦 **Rate Limiting:** Соблюдение лимитов VK API (не более 3 запросов в секунду).
- 🔄 **Статусы ставок:** В таблице видно всё: `лидер`, `перебита`, `уведомлен`, `низкая`.

### Инструменты администратора
- 🤖 **Симулятор:** Встроенный бот, который создает тестовые посты и активно торгуется сам с собой, проверяя всю логику в "боевом" режиме.
- 🐛 **Debug Mode:** Переключатель подробности логов (`DEBUG_VK_API`) — от полной трассировки до "только важное".
- 🚑 **Самодиагностика:** Функции проверки здоровья системы и "Турбо-очистка" зависших очередей.

---

## 🛠 Установка и Настройка (Мастер 4 шагов)

Бот настраивается автоматически. Вам не нужно вручную создавать сервера в ВК.

### 1. Подготовка
1.  Создайте Google Таблицу и откройте **Apps Script**.
2.  Выполните **Deploy** -> **New Deployment** (Web App, Everyone) и скопируйте URL.

### 2. Подключение (🔐 Настройки авторизации)
Процесс разбит на 4 простых шага прямо в окне настроек:

*   **Шаг 1: Группа.** Вставьте ссылку на ваше сообщество (например, `vk.com/club123`).
*   **Шаг 2: Admin Token.** Нажмите синюю кнопку, разрешите доступ и вставьте адресную строку браузера в поле ниже. *Нужен для создания Callback-сервера.*
*   **Шаг 3: Group Token.** Нажмите оранжевую кнопку и сделайте то же самое. *Нужен боту для ответов от имени группы.*
*   **Шаг 4: Подключение.** Нажмите "Подключить и проверить". 
    *   *Скрипт сам создаст сервер, получит код подтверждения и выполнит "тихую" проверку прав (создаст и сразу удалит тестовый пост).*

### 3. Запуск
1.  Нажмите **VK Auction** → **🚀 Мастер настройки** для создания листов.
2.  Нажмите **VK Auction** → **🔄 Сбросить триггеры**.

---

## 🔑 Почему два токена?
Для максимальной надежности мы разделили права:
1.  **Admin (User) Token**: Имеет право "командовать" группой (менять настройки API). Используется только один раз при подключении.
2.  **Group Token**: Позволяет боту писать сообщения и комментарии. Используется постоянно в ходе аукциона.

---

## 🐛 Диагностика и Тесты
Если что-то не работает, используйте новые инструменты в меню:
*   **🔬 ТЕСТЫ → 🔑 Проверить права токенов (Full)**: запустит полную проверку (пост + коммент + удаление).
*   **🔧 СЕРВИС → 👤 Диагностика владельцев**: покажет, чьи это токены и какие у них роли.


## 🤖 Симулятор

Симулятор позволяет проверить работу бота без реальных участников.

**Как это работает:**
1.  Бот публикует реальный пост на стене вашей группы.
2.  Бот начинает писать комментарии (ставки) от вашего имени (или имени группы).
3.  Основной скрипт обрабатывает их как настоящие ставки: перебивает, уведомляет, продлевает лот.

**Запуск:**
Меню **VK Auction** → **🤖 СИМУЛЯТОР** → **▶️ Запустить один цикл**.

---

## 📊 Описание листов таблицы (Куда смотреть?)

В таблице много листов, вот зачем нужен каждый из них:

1.  **Лоты (`Config`)** — **Главный лист.** Здесь хранятся все аукционы.
    *   Сюда бот записывает новые посты с хэштегом `#аукцион`.
    *   Здесь обновляется `current_price` (текущая цена) и `leader_id` (кто выигрывает).
    *   Здесь видно `deadline` (когда конец).

2.  **Ставки (`Bids`)** — **История торгов.**
    *   Сюда попадает **каждая** попытка сделать ставку.
    *   Колонка `status` показывает судьбу ставки:
        *   `лидер` — сейчас выигрывает.
        *   `перебита` — была лидером, но её перебили.
        *   `уведомлен` — перебита, и бот успешно ответил человеку в VK.
        *   `низкая` — человек поставил мало, бот его отшил.

3.  **Очередь Событий (`EventQueue`)** — **Технический лист (Входящие).**
    *   Сюда мгновенно падают все сырые сигналы от ВКонтакте (комментарии, посты).
    *   Бот разгребает этот лист раз в минуту.
    *   Если тут статус `pending` — значит, бот еще не обработал событие. Если `processed` — всё ок.

4.  **Очередь (`NotificationQueue`)** — **Технический лист (Исходящие).**
    *   Сюда попадают задания "Отправить сообщение".
    *   Бот берет их отсюда и отправляет, чтобы не нарушать лимиты VK.

5.  **Победители (`Winners`)** — **Итоги.**
    *   Заполняется только когда аукцион завершен (по дедлайну).
    *   Здесь список тех, кто должен платить.

6.  **Журнал (`Logs`)** — **Ошибки и инфо.**
    *   Если что-то сломалось, смотрите сюда.
    *   Если `DEBUG_VK_API = TRUE`, тут будет вообще всё.

7.  **Настройки (`Settings`)** — **Управление.**
    *   Здесь можно менять **шаблоны текстов** для всех ответов бота (низкая ставка, неверный шаг, победа и т.д.).
    *   Здесь находятся **переключатели (ВКЛ/ВЫКЛ)** для ключевых функций: проверка подписки, отправка ЛС победителям, ответы на ошибки и подробное логирование.

8.  **Статистика (`Statistics`)** — **Графики (опционально).**
    *   Техническая статистика работы системы.

---

## 🐛 Отладка и Логи

Если что-то идет не так:

1.  **Проверьте Журнал (`Logs`):** Там пишутся все ошибки.
2.  **Режим отладки:**
    *   В листе **"Настройки"** поставьте `DEBUG_VK_API` = **`TRUE`**.
    *   Бот начнет писать в журнал каждое своё действие (входящие запросы, парсинг, ответы API).
    *   Для обычной работы поставьте **`FALSE`** (или удалите настройку).
3.  **Если очередь зависла:**
    *   Запустите функцию `forceClearAllQueues` из файла `Debug.gs`. Она принудительно разгребет все накопившиеся события.

---

## 📁 Структура проекта

*   **`Code.gs`** — Сердце бота. Прием Webhook (`doPost`), маршрутизация, обработка ставок.
*   **`Vk.gs`** — Библиотека работы с VK API.
*   **`Sheets.gs`** — Работа с таблицами (чтение, запись, умное обновление).
*   **`AuctionSimulator.gs`** — Логика симулятора.
*   **`Debug.gs`** — Инструменты диагностики и ручного запуска.
*   **`Monitoring.gs`** — Запись статистики.

---

*Разработано на базе Google Apps Script для автоматизации VK-аукционов.*
