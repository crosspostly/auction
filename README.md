# VK Auction Bot

Автоматизированный бот для проведения аукционов во ВКонтакте на базе Google Apps Script.

## Возможности

- 🔍 Автоматическое обнаружение постов-лотов по хэштегу `#аукцион`
- 💰 Отслеживание ставок в комментариях в режиме реального времени и **полная запись всех ставок в отдельный лист**
- 📢 Уведомления участникам о перебитых ставках и **о слишком низких ставках (менее текущей)**
- ⏰ Автоматическое продление дедлайна при ставке в последние 10 минут
- 🏆 Определение победителей и автоматическая отправка уведомлений с реквизитами
- 📊 Сводные отчёты администраторам по итогам аукциона
- 📦 Расчёт стоимости доставки по количеству лотов
- **🛡️ Валидация ставок по максимальной допустимой сумме (`max_bid`)**
- **🤖 Встроенный симулятор аукциона для тестирования и генерации данных**

## Технологии

- **Backend**: Google Apps Script (JavaScript ES6+)
- **API**: VK Callback API + VK API методы
- **Хранилище**: Google Sheets
- **Планировщик**: Time-driven triggers в GAS

## Установка

### 1. Создание Google Apps Script проекта

1. Откройте [Google Sheets](https://sheets.google.com) и создайте новую таблицу
2. Перейдите в меню **Расширения** → **Apps Script**
3. Скопируйте содержимое файлов из репозитория:
   - `Code.gs`
   - `Sheets.gs`
   - `VkApi.gs` (был `Vk.gs`)
   - `Monitoring.gs`
   - `AuctionSimulator.gs`
   - `appsscript.json`

### 2. Настройка VK Callback API

1. Перейдите в настройки вашего сообщества ВКонтакте
2. Выберите раздел **Работа с API** → **Callback API**
3. Создайте новый Callback API сервер
4. URL сервера: скопируйте URL из Apps Script после публикации (Deploy → New deployment → Web app)
5. Версия API: 5.131 (или выше)
6. Включите следующие события:
   - `wall_post_new` (новые записи на стене)
   - `wall_reply_new` (комментарии к записям)
   - `message_new` (личные сообщения)
7. Сохраните **Secret key** и **Confirmation string**

### 3. Первоначальная настройка

1. В Google Sheets откройте меню **VK Auction**
2. Выберите **Настройка авторизации (VK)**
3. Введите следующие параметры:
   - `VK_TOKEN`: токен доступа с правами `messages`, `wall`, `groups` ([получить токен](https://vkhost.github.io))
   - `GROUP_ID`: ID вашего сообщества (число без минуса)
   - `CONFIRMATION_CODE`: строка подтверждения из Callback API
   - `VK_SECRET`: Secret key из Callback API
   - `PAYMENT_PHONE`: телефон для оплаты (СБП)
   - `PAYMENT_BANK`: название банка
   - `ADMIN_PASSWORD`: Пароль для доступа к админским функциям (по желанию)

4. Выберите **Мастер настройки** для создания необходимых таблиц и установки триггеров.

### 4. Публикация веб-приложения

1. В редакторе Apps Script нажмите **Deploy** → **New deployment**
2. Выберите тип: **Web app**
3. Настройки:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. Скопируйте URL веб-приложения и используйте его в настройках Callback API

### 5. Настройка шаблонов и общих параметров

В листе **Настройки** (`Settings`) настройте:

- `DEBUG_VK_API`: `TRUE` для подробного логирования запросов к VK API.
- `bid_step_enabled`: `TRUE` для включения проверки шага ставки.
- `bid_step`: Размер шага ставки (например, `50` руб).
- `min_bid_increment`: Минимальная надбавка к текущей цене.
- `max_bid`: **Максимально допустимая ставка (защита от опечаток), например, `1000000`**.
- `delivery_rules`: Правила доставки (JSON формат: `{"1-3": 450, "4-6": 550, "7+": 650}`).
- `order_summary_template`: Шаблон сообщения победителю.
- **Обратите внимание**: Некоторые чувствительные настройки (как токены) хранятся в `PropertiesService` и настраиваются через меню **VK Auction → Настройка авторизации (VK)**.

## Формат поста-лота

Для автоматического распознавания ботом, пост должен содержать:

*   Хэштег `#аукцион` (регистр не важен, может быть часть другого хэштега, например `#аукцион@yourgroup`)
*   Номер лота (`№` или `No.` или `Number`)
*   Стартовую цену и опционально шаг ставки (`Старт [цена]р и шаг [шаг]р`)
*   Дедлайн (`Дедлайн ДД.ММ.ГГГГ в ЧЧ:ММ по МСК`)

**Пример:**

```
#аукцион@dndpotustoronu №1840
При поддержке GABRIGAME-WORKSHOP!
Дедлайн 13.12.2025 в 21:00 по МСК!
🎁Лот - на картинке. + миниатюра идет с красивой, текстурной базой.

👀Старт 150р и шаг - 50р.
Каждая миниатюра аукциона масштабом 32-35мм.
...
```

## Формат ставки

Участники делают ставки в комментариях, указывая только сумму: `1500`

## Команды в личных сообщениях

- Отправьте `аукцион` боту, чтобы получить список выигранных лотов
- Отправьте адрес доставки после получения уведомления о победе

## Логика работы

1.  **Обнаружение лота**: Бот отслеживает новые посты с хэштегом `#аукцион` и парсит детали лота (номер, стартовая цена, шаг, дедлайн).
2.  **Обработка ставок**: При новых комментариях с числом бот проверяет валидность ставки:
    *   Ставка должна быть выше текущей цены.
    *   Ставка должна быть кратна шагу (если включена опция `bid_step_enabled`).
    *   **Ставка не должна превышать `max_bid`.**
    *   Валидные ставки записываются в лист **"Ставки"** и обновляют текущего лидера в листе **"Лоты"**.
3.  **Продление дедлайна**: Если валидная ставка сделана в последние 10 минут до дедлайна, аукцион автоматически продлевается на 10 минут.
4.  **Уведомления**: 
    *   Предыдущий лидер получает уведомление "Ваша ставка перебита".
    *   **Пользователь, сделавший слишком низкую ставку, получает дружелюбное уведомление с текущей ценой.**
5.  **Финализация**: По истечении дедлайна бот определяет победителя (или помечает лот как непроданный, если ставок не было) и отправляет победителю личное сообщение с реквизитами.
6.  **Сводка админам**: После завершения всех лотов администраторы получают полный отчёт (если настроено).

## Симулятор Аукциона (`AuctionSimulator.gs`)

Симулятор позволяет генерировать тестовые посты и комментарии в вашей группе VK для отладки и проверки работы бота. Он поддерживает:

*   Создание постов-лотов по реалистичному шаблону.
*   Имитацию ставок от **нескольких различных тестовых пользователей**.
*   Генерацию сценариев с валидными, слишком частыми, равными, низкими и некратными шагу ставками.
*   Логирование всех действий симулятора в лист **`Статистика`**.

**Как использовать симулятор:**
1.  Откройте Google Таблицу.
2.  Через меню **VK Auction > СИМУЛЯТОР** вы можете:
    *   **Запустить один цикл симуляции**: Создаст 1 пост и серию комментариев.
    *   **Включить/Остановить ежечасный запуск**: Автоматически запускает симуляцию каждый час (макс. 5 постов за цикл).
    *   **Сбросить счетчик постов**: Сбрасывает лимит постов для ежечасного запуска.

## Структура данных

### Листы Google Sheets

-   **Лоты** (`Config`): Активные и завершённые лоты, включая `bid_step`.
-   **Ставки** (`Bids`): **Полный журнал всех ставок**, включая `bid_id`, `lot_id`, `user_id`, `bid_amount`, `timestamp`, `comment_id`.
-   **Победители** (`Winners`): Список победителей аукционов.
-   **Настройки** (`Settings`): Параметры конфигурации системы.
-   **Статистика** (`Statistics`): **Журнал всех ключевых событий системы и симулятора**.
-   **Очередь Событий** (`EventQueue`): Очередь входящих событий от VK для асинхронной обработки.
-   **Очередь** (`NotificationQueue`): Очередь исходящих уведомлений пользователям.
-   **Журнал** (`Logs`): Общие информационные логи и логи ошибок.

## Безопасность

⚠️ **Важно**: Чувствительные данные (токены, реквизиты) хранятся в `PropertiesService` и **не должны** записываться в таблицу `Settings`. Для управления этими настройками используйте меню **VK Auction → Настройка авторизации (VK)**.

## Поддержка

При возникновении проблем:
1.  Проверьте лист **`Журнал`** для просмотра ошибок.
2.  Используйте **VK Auction → Проверить соединение** для тестирования подключения к VK API.
3.  **VK Auction → СИМУЛЯТОР** для генерации тестовых данных и отладки.

## Лицензия

MIT License